# Expanded Site Validation Notes

The public Framework, Products, Insights, article, and Contact routes were visually checked at desktop size. The Framework, Insights, and Contact routes were also checked at a 390px mobile viewport; each kept a readable single-column layout, accessible navigation trigger, and usable contact inputs.

The production build, TypeScript check, and five Vitest checks pass. Raw server responses were verified to include the article body, article Open Graph type, BlogPosting JSON-LD, and the fallback-backed sitemap URL for the first Insights article.

The server-rendering and technical SEO pass verifies one title, description, canonical URL, Open Graph title, Twitter card, and visible server-rendered application shell on every public route: Home, Framework, Products, Insights, the sample insight detail page, and Contact. The page source also includes Organization and WebSite structured data, BlogPosting schema for articles, an indexable `robots.txt`, and a sitemap that excludes the private studio route. Canonical URLs, sitemap URLs, and schema now use `https://coreweaverlabs.com/` consistently.

The Work With Us page was simplified to a three-step guided qualification flow. Desktop and 390px mobile views confirm that the first prompt, tap targets, progress indicator, and reassurance copy remain clear without making visitors complete a traditional long form. The new qualification-message helper is covered by two focused Vitest cases.

The managed database migration later completed successfully. The `users`, `insights`, and `contact_submissions` tables were confirmed present. With owner approval, a clearly labeled live Work With Us verification inquiry was accepted and persisted through the public contact procedure, with no owner-notification delivery warning recorded by the server. The temporary verification record was then removed to keep the inquiry list clean.

A second approved browser-driven verification completed the rendered three-step Work With Us flow against the live backend. The browser displayed the real success message, the generated qualification payload persisted in `contact_submissions`, and the server logged `Owner notification accepted` for that inquiry. The browser-verification record was removed after confirmation.

The Work With Us flow now animates step changes with a short directional opacity-and-position transition and disables nonessential motion for visitors who request reduced motion. Insight article pages now expose visible, labelled LinkedIn and X sharing controls with encoded canonical destinations. Desktop and 390px mobile reviews confirmed the controls remain clear and finger-friendly; raw SSR output also contains both sharing URLs.

The new Mason Nguyen author profile and article byline links were reviewed at desktop and 390px mobile widths. The author profile retains a readable hierarchy, compact authority topics, and a clearly separated article list. The linked article byline is visible without competing with the title or sharing controls.

The author profile now labels its LinkedIn, Substack, X, and GitHub links as Coreweaver Labs channels rather than personal accounts. Raw server-rendered HTML contains the authority links with safe external-link semantics, while desktop and mobile checks confirm the compact links remain readable and wrap cleanly.

The case-study index, governance preview, and private evidence intake form were reviewed at desktop size. The public index makes the review standard clear, the preview is conspicuously labelled as non-publishable, and the intake groups all seven required fields into a readable four-part evidence record.

At 390px, the public standards page, non-publishable preview, and seven-field intake all retain a readable single-column flow with usable form inputs and visible governance labels. Raw SSR checks confirm that the public index is crawlable and sitemap-listed, while the preview and private intake are noindexed and excluded from the sitemap. The rendered intake test confirms that a complete authorized record reaches the private-review success state without becoming public content.

The runtime sitemap retains a static fallback whenever the Insights table is temporarily unavailable, so public crawlability remains intact during a database service interruption.

The supplied Swell compliant case-study template was mapped to the private intake and approved-record contract. The intake now requires source naming, an exact supportable finding, optional metric context, reporting period, review date, a source-owner approval reference, written publication authorization, and affirmative privacy and claim-safety checks. The reusable approved-record layout now presents the work, verified finding, evidence basis, conditional metric context, and a specific non-guarantee statement. It remains a preview until an approved public record exists.

With owner approval, one clearly labelled temporary intake was submitted through the live public tRPC procedure. The server accepted the owner notification, and one row appeared in `case_study_intakes` with `received` status and all three authorization/privacy/claim-safety confirmations enabled. No matching row appeared in `case_studies`; the matching unapproved public URL returned HTTP 404 with `noindex`, and the test label was absent from the sitemap. The temporary private row was then deleted and a follow-up query returned no matching row.

The final validation pass completed successfully: TypeScript reported no errors, all 17 automated tests passed, and both client and SSR production bundles built. At 390px, the evidence-pending index, non-publishable preview, and expanded private intake preserve readable single-column hierarchy and usable controls. Raw SSR checks returned 200 for the public index with its evidence-pending body; the private intake and preview each returned 200 with `noindex`; the sitemap includes `/case-studies` and excludes both private routes.

The migration history was rechecked for reproducibility. `drizzle/0004_little_rhodey.sql` includes the first-statement addition of `case_studies.sourceName` as well as the related metric and private-review fields. A subsequent Drizzle generation run reported no pending schema changes, confirming that the tracked migrations match the current 18-column approved-record table and 19-column private-intake table.

The private Studio queue now includes an admin-only reviewer-handoff form for source-owner identity, Swell reviewer, privacy reviewer, planned publication date, and a handoff state. Its server procedure is protected by `adminProcedure`; a record cannot be marked `ready` until every handoff field is present. Even in the ready state, the UI and backend preserve the separation from `case_studies`: no approved public record, route, or sitemap entry is created automatically. The new migration is tracked in `drizzle/0005_petite_scarlet_witch.sql`; Drizzle reports no pending changes. The final check, 18-test suite, and client/SSR production build all pass.

The integrated GTM foundation adds a public `/services` hub for the Signal Intelligence Audit, GTM Enablement Sprint, and Representation Operations service lines. Its request form accepts either a service inquiry or support request and stores only a private `gtm_requests` record; it does not create an account, opportunity, support case, engagement, public client profile, or published claim automatically. The protected `/workspace` provides explicit account, contact, opportunity, support-case, and cross-functional work creation flows for sales, customer support, operations, marketing, research, and design. The private data model is tracked in `drizzle/0006_smiling_orphan.sql` and covers the six new GTM tables without seed data.

The GTM hub rendered successfully at desktop and 390px mobile widths, preserving the service hierarchy and usable private request form. The protected workspace rendered with empty operational states rather than fabricated data. Raw SSR checks returned HTTP 200 for `/services`, found the primary service message and canonical URL, found no `noindex`, and confirmed `/services` appears in the sitemap. `/workspace` returned HTTP 200 with `noindex` and does not appear in the sitemap. Type checking passed; the 21-test suite passed, including the rendered public-request success boundary; both client and SSR production builds passed. A final Drizzle generation run found no pending schema changes.

The final GTM workspace revision exposes private account and contact registries with lifecycle controls, as well as a dedicated request-triage view with an explicit private owner and `new`, `triaged`, or `closed` state. The triage copy and implementation preserve the manual boundary: an operator must separately create any account, opportunity, or support case after reviewing a request. Migration `drizzle/0007_wide_molten_man.sql` adds only the nullable private request-owner field. The rendered workspace shows the registries and triage view in empty state, without seeding customer data. The final type check, 21-test suite, client/SSR production build, and Drizzle parity check all passed.

An accessibility-focused review confirmed that the public request form uses named labels for its inputs, request-type controls, and submit control; a rendered keyboard test verifies focus moves from the Name field to Business email in the expected sequence. The desktop and mobile service-hub reviews show readable service hierarchy, visible empty states, and usable controls. The workspace’s private registries and triage controls are clearly labelled, remain in the protected dashboard, and show empty states when no actual operating data exists.

The GTM protected-boundary check called `gtm.listRequests` without an authenticated admin session and received HTTP 403 with the application’s `FORBIDDEN` response. Final SSR checks after the registry and triage changes returned HTTP 200 for `/services` with the server-rendered body and canonical `https://coreweaverlabs.com/services`, no `noindex`, and a sitemap entry. `/workspace` returned HTTP 200 with `noindex` and is excluded from the sitemap. The final type check, full 22-test suite, client/SSR production builds, and Drizzle parity run completed successfully.

The protected-workspace accessibility test renders a private account, contact, and triage record and verifies the keyboard path from Account Owner to Account State to Save. It also confirms accessible names for contact Status, Private owner, Triage state, and Save triage. The full post-check suite passed with 23 tests, zero TypeScript errors, and successful client/SSR production bundles.

The workspace keyboard check was expanded to verify the complete focus path across the rendered private records: Account Owner → Account State → Save → Contact Status → Private owner → Triage state → Save triage. The focused test and the final validation suite both passed. The final project validation remains at zero TypeScript errors, 23 passing automated tests, and successful client and SSR production builds.

The public `/services` hub now presents research-based mid-market B2B starting estimates pending owner approval: SEO from $6,500/month, Content Marketing from $7,500/month, and Paid Ads from $7,500/month with client-funded media spend kept separate. The pricing basis is documented in `pricing-rationale.md` from current Clutch service-pricing guides. The page explains premium differentiation through research, connected channels, reviewable decisions, and explicit non-guarantee language; it makes no client, ranking, lead, revenue, or ROAS claim. The supplied URL dashboard is recorded separately as an internal inventory in `urldashboard-review.md` and is not used as portfolio or client proof.

The presence-first strategy is documented in `presence-first-strategy.md`. It sets a research thesis, source hierarchy, claim ledger, human-accountability standard, weekly/monthly/quarterly cadence, three sourceable research briefs, and leading indicators that distinguish owned publishing quality from independent recognition. The research-methods page at `/research` implements this standard publicly without representing the agenda as completed research or client proof. It includes named-method, source, claim-review, correction, and authorized-evidence language; public citations to Google’s people-first guidance; and explicit statements that client evidence is never published automatically.

Desktop and 390px mobile reviews confirmed readable research-principle, source-standard, governance, and agenda sections. Raw SSR checks returned HTTP 200 for `/research`, found the research title and canonical URL, found no `noindex`, and confirmed its sitemap entry. The public route is linked from both the marketing navigation and Insights. Type checking and the 27-test suite passed, including focused coverage that verifies the page’s sourceable-publishing and no-preclaimed-findings language.

The AI support guide is server-side only and uses `gpt-5-mini` for structured classification. It never creates a request, account, opportunity, case study, or public record. The model supplies only a route, service category, and urgency; customer-facing reply text and the returned summary are fixed, approved language, so model output cannot invent policies or next-step promises. A controlled non-sensitive live request returned the sales/SEO route and the deterministic private-form message. The guide directs the visitor to the existing private request form, which still requires manual sales or support triage.

An initial Streamdown-based chat presentation caused SSR to load a KaTeX stylesheet through the development server, producing a public-services render failure. It was replaced with an SSR-safe native, labelled conversation interface while preserving the AI routing flow. The repaired `/services` page rendered correctly at desktop and 390px mobile widths. Final raw SSR checks returned 200 for `/services` with the premium title and starting-price text, no `noindex`, and a sitemap entry; `/workspace` remained `noindex` and sitemap-excluded. Type checking, the 26-test suite, and client/SSR production builds passed. Migration `drizzle/0008_adorable_kate_bishop.sql` safely extends the private opportunity enum with the new service lines.

The private Insights workflow now records a publication type (`article`, `research_brief`, or `field_brief`), source references, method note, named claim reviewer, and claim-review confirmation. The server accepts incomplete research records only as drafts. A research or field brief cannot publish unless all four review requirements are present. The `0009_flashy_sir_ram.sql` migration was reviewed as additive only and applied successfully to the `insights` table. For approved research records, the public Insight detail page transparently displays the method note, reviewer confirmation, and source references without rendering supplied text as HTML.

Final validation passed `pnpm run check`, all **29 Vitest tests** across 11 files, and the client plus SSR production builds. The support-routing test intentionally logs its exercised human-review fallback; this is expected test output. The production build continues to report the existing non-blocking client bundle-size advisory above 500 kB.

The remaining operating foundation is documented without fabricating client work or market outcomes. `delivery-playbooks.md` defines named discovery, baseline, quality-review, reporting, and cross-functional handoff gates for SEO, Content Marketing, and Paid Ads. `proof-and-review-program.md` defines an authorized evidence route and neutral, optional client-review practice; it explicitly prohibits scripting, buying, gating, or rewarding favorable reviews. `authority-scorecard.md` establishes a manual quarterly scorecard with source, denominator, limitations, owner, and decision requirements, while leaving live measurement pending actual owner-approved systems and real evidence.

`ai-friendly-positioning-package.md` provides an implementation-ready, claim-safe positioning package. It recommends pairing the emotional homepage tagline with the explicit statement “Evidence-led SEO, Content Marketing, and Paid Ads for mid-market B2B teams”; provides an Organization/WebSite plus three-service OfferCatalog JSON-LD pattern; specifies matching title and metadata language; drafts a server-rendered hero; and defines an authorized, machine-readable case-study Evidence Record and Report JSON-LD pattern. It expressly excludes unverified ratings, reviews, awards, prices, performance figures, confidential sources, and client identifiers from structured data.

Desktop and 390px mobile visual reviews of the new `/ai-data-policy` route confirmed readable heading hierarchy, side-by-side policy sections that collapse to a single column on mobile, visible external provider-documentation links, a clear protected-information boundary, and a usable contact action. The public page distinguishes retrieval-oriented AI search use from model-training and bulk-reuse restrictions without treating crawler directives as access control.

The final technical check returned HTTP 200 for `/ai-data-policy`, confirmed its rendered indexable robots meta tag, canonical URL, title, and sitemap entry, and verified `robots.txt` contains the GPTBot, OAI-SearchBot, and Google-Extended policy tokens. The updated directives permit general crawl/search use and explicitly allow known retrieval-oriented OpenAI and Anthropic agents while disallowing the named GPTBot, ClaudeBot, Google-Extended, and CCBot training/model-development controls. Type checking, the full 12-file/30-test suite, and client/SSR production builds passed; the build retains the existing non-blocking client bundle-size advisory.

Desktop and 390px mobile visual reviews of the new `/faq` route confirmed the buyer-priority header order, readable dark-terminal FAQ treatment, searchable category controls, visible answer counts, accessible accordion hierarchy, and an intact contact handoff. At mobile widths, the sticky desktop sidebar becomes an in-flow search and topic-control block, while each answer remains legible and the CTA remains reachable.

`brand-strengths.md` records the supportable strengths currently available for external positioning: evidence-led publication governance, mid-market B2B focus, connected SEO/Content Marketing/Paid Ads services, governed AI-facing representation, private-to-public proof discipline, and manual operational continuity. The primary navigation now reflects buyer sequence: Services, How it works, Case studies, Insights, Research, and FAQ; Products remains reachable in the footer. The new `/faq` page includes five categories and 18 source-safe answers, accessible Radix accordions, topic filtering, search, FAQPage and BreadcrumbList JSON-LD, a canonical title and description, sitemap inclusion, and a contact handoff. Final validation passed `pnpm run check`, **32 tests across 13 files**, and client/SSR production builds. `/faq` returned HTTP 200 with indexable robots metadata, a canonical URL, FAQPage markup, and a sitemap entry; the existing non-blocking client bundle-size advisory remains.

Desktop and 390px mobile reviews of the clarified homepage confirm that the emotional tagline remains prominent while its practical audience-and-service explanation appears immediately below it. The formerly specialized vocabulary is now defined in its first buyer-facing context: a signal is the information around a business; an operating system is repeatable roles, source material, and review routines; Citation Intelligence is an AI answer and source review; governance is the review rules and approval habits that keep public claims accurate; and a Knowledge System combines these elements as shared current source material with ownership. The updated copy remains readable across the existing desktop and mobile editorial layouts.

The final homepage SSR check returned HTTP 200, found the revised mid-market B2B clarity copy, confirmed the canonical homepage URL, and found the rendered indexable robots meta tag. The complete quality gate passed `pnpm run check`, **33 tests across 14 files**, and client/SSR production builds; the existing non-blocking client bundle-size advisory remains.
