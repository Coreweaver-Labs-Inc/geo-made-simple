import { CheckCircle2, LoaderCircle } from "lucide-react";
import React from "react";
import { useForm } from "react-hook-form";
import { MarketingShell, SectionLabel } from "@/components/SiteChrome";
import { SeoHead } from "@/components/SeoHead";
import { trpc } from "@/lib/trpc";

type IntakeValues = {
  clientLabel: string;
  sourceName: string;
  sourceReference: string;
  supportableFinding: string;
  metricDefinition: string;
  scope: string;
  reportingStart: string;
  reportingEnd: string;
  reviewDate: string;
  sourceOwnerApproval: string;
  publicationAuthorization: string;
  replyEmail: string;
  authorizationConfirmed: boolean;
  privacyReviewConfirmed: boolean;
  claimReviewConfirmed: boolean;
  formWebsite: string;
};

export default function CaseStudyIntake() {
  const form = useForm<IntakeValues>({ defaultValues: { clientLabel: "", sourceName: "", sourceReference: "", supportableFinding: "", metricDefinition: "", scope: "", reportingStart: "", reportingEnd: "", reviewDate: "", sourceOwnerApproval: "", publicationAuthorization: "", replyEmail: "", authorizationConfirmed: false, privacyReviewConfirmed: false, claimReviewConfirmed: false, formWebsite: "" } });
  const submit = trpc.caseStudies.submitIntake.useMutation({ onSuccess: () => form.reset() });
  const submitted = submit.isSuccess;

  return <MarketingShell><SeoHead title="Case Study Evidence Intake | Coreweaver Labs" description="Private client intake for approved case-study evidence and publication authorization." path="/case-study-intake" noIndex /><main><section className="intake-hero section-pad"><SectionLabel>Case study evidence intake</SectionLabel><h1>One approved record. No unsupported claim.</h1><p className="page-lede">Use this private intake to provide the evidence required before a case study can enter editorial review. Submission creates a review record only; it does not publish anything.</p></section><section className="intake-layout section-pad section-rule"><aside className="intake-aside"><p className="page-kicker">Seven core fields + safety checks</p><ol><li>Client label</li><li>Source name and record</li><li>Exact supportable finding</li><li>Metric context, if a metric appears</li><li>Scope and reporting window</li><li>Review date and source-owner approval</li><li>Written publication authorization</li><li>Privacy and claim-safety confirmation</li></ol><p>Use an authorized anonymous label whenever the client name cannot be published. Do not submit credentials, personal data, or confidential figures that are not approved for public use.</p></aside>{submitted ? <div className="form-success case-intake-success" role="status" aria-live="polite"><CheckCircle2 size={26} aria-hidden="true" /><div><strong>Your evidence record is in review.</strong><p>Nothing has been published. Coreweaver Labs will compare the record, source, safety checks, and authorization before any draft is created.</p></div></div> : <form className="case-intake-form" onSubmit={form.handleSubmit(values => submit.mutate({ ...values, authorizationConfirmed: true, privacyReviewConfirmed: true, claimReviewConfirmed: true }))} noValidate><div className="form-honeypot" aria-hidden="true"><label htmlFor="case-website">Leave this field empty</label><input id="case-website" tabIndex={-1} autoComplete="off" {...form.register("formWebsite")} /></div><fieldset><legend>01 — Client label</legend><label className="form-field">Client name or authorized anonymous label<input className="contact-input" placeholder="Example: Enterprise healthcare platform" {...form.register("clientLabel", { required: "Please provide an approved client label.", minLength: 3 })} />{form.formState.errors.clientLabel && <em>{form.formState.errors.clientLabel.message}</em>}</label></fieldset><fieldset><legend>02 — Source and finding</legend><label className="form-field">Source name<input className="contact-input" placeholder="Example: Approved Q1 visibility report" {...form.register("sourceName", { required: "Please name the supporting source.", minLength: 2 })} /></label><label className="form-field">Source URL or document reference<textarea className="contact-textarea" rows={3} placeholder="https://… or Report name, page/tab, owner, and access location" {...form.register("sourceReference", { required: "Please identify the source.", minLength: 8 })} /></label><label className="form-field">Exact supportable finding<textarea className="contact-textarea" rows={4} placeholder="State precisely what the source supports, including the metric, baseline, and method where relevant." {...form.register("supportableFinding", { required: "Please state the exact supportable finding.", minLength: 30 })} /></label><label className="form-field">Metric definition or comparison basis <i>Required whenever the finding includes a metric</i><textarea className="contact-textarea" rows={3} placeholder="Unit, denominator, comparison point, and method used by the source." {...form.register("metricDefinition")} /></label></fieldset><fieldset><legend>03 — Scope and reporting window</legend><label className="form-field">Scope of work<textarea className="contact-textarea" rows={3} placeholder="Services delivered, channels or markets covered, and relevant exclusions." {...form.register("scope", { required: "Please define the scope.", minLength: 20 })} /></label><div className="form-grid"><label className="form-field">Reporting start<input className="contact-input" type="date" {...form.register("reportingStart", { required: "Required" })} /></label><label className="form-field">Reporting end<input className="contact-input" type="date" {...form.register("reportingEnd", { required: "Required" })} /></label></div><label className="form-field">Evidence review date<input className="contact-input" type="date" {...form.register("reviewDate", { required: "Required" })} /></label></fieldset><fieldset><legend>04 — Approval and safety checks</legend><label className="form-field">Source-owner approval reference<textarea className="contact-textarea" rows={3} placeholder="Approval message, contract clause, release ID, or other reference from the source owner." {...form.register("sourceOwnerApproval", { required: "A source-owner approval reference is required.", minLength: 10 })} /></label><label className="form-field">Written publication authorization record<textarea className="contact-textarea" rows={4} placeholder="Approver name and role, approval date, and the approved client label and finding wording." {...form.register("publicationAuthorization", { required: "Written authorization is required before publication.", minLength: 30 })} /></label><label className="intake-check"><input type="checkbox" {...form.register("authorizationConfirmed", { validate: value => value || "Please confirm that written publication authorization is included above." })} /> <span>I confirm that this record includes written authorization for the proposed publication scope and wording.</span></label><label className="intake-check"><input type="checkbox" {...form.register("privacyReviewConfirmed", { validate: value => value || "Please confirm the privacy review." })} /> <span>I confirm that unapproved personal data, credentials, confidential figures, and regulated data have been removed or redacted.</span></label><label className="intake-check"><input type="checkbox" {...form.register("claimReviewConfirmed", { validate: value => value || "Please confirm the claim-safety review." })} /> <span>I confirm that forecasts, targets, mock data, and unsupported causal claims are excluded from the proposed public copy.</span></label>{(form.formState.errors.authorizationConfirmed || form.formState.errors.privacyReviewConfirmed || form.formState.errors.claimReviewConfirmed) && <em className="form-error-inline">Complete each required authorization and safety confirmation.</em>}<label className="form-field">Reply email <i>Optional — for clarification only</i><input className="contact-input" type="email" autoComplete="email" {...form.register("replyEmail")} /></label></fieldset>{submit.error && <div className="form-alert" role="alert">{submit.error.message}</div>}<button className="button button-primary" type="submit" disabled={submit.isPending}>{submit.isPending ? <><LoaderCircle className="spin" size={16} /> Sending record</> : "Submit for review"}</button><p className="form-note">Submission is private. It does not create a public case study.</p></form>}</section></main></MarketingShell>;
}
